<!doctype html>
<html lang="en" class="no-js">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>{{title}} - {{helper_config 'siteName'}}</title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta content="{{#if robots_tag}}{{robots_tag}}{{else}}index,follow{{/if}}" name="robots">
        <meta name="description" content="{{summary}}">
        {{#if css_file}}<link rel="stylesheet" href="{{assets}}/css/{{css_file}}.css">{{/if}}
        {{> open-graph-markup }}
        {{> twitter-card }}
        <link rel="canonical" href="{{helper_current_page_absolute_url}}">
        <link href="{{assets}}/img/favicon.png" rel="shortcut icon">
        <script>(function(H){H.className=H.className.replace(/\bno-js\b/,'js')})(document.documentElement)</script>
    </head>

    <body class="{{body_class}}">

        {{> body }}

        {{#if js_file}}<script src="{{assets}}/js/vendor/babel-polyfill.js"></script>{{/if}}
        {{#if js_file}}<script src="{{assets}}/js/{{js_file}}.js"></script>{{/if}}
        {{> google-analytics-tracking-code }}

    </body>
</html>